import Link from 'next/link';
import React from 'react';
import { FaChartPie, FaCog, FaHome, FaMobileAlt } from 'react-icons/fa';

const AppDtlsLayout = ({children}) => {
    return (
        <div >
 
        {children}
        </div>
    );
};

export default AppDtlsLayout;